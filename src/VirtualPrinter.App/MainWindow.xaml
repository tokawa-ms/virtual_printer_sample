<Window x:Class="VirtualPrinter.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Virtual Print Demo"
        Width="520" Height="320"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanMinimize">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" FontSize="18" FontWeight="Bold"
                   Text="Virtual Print Demo"/>

        <TextBlock Grid.Row="1" Margin="0,8,0,0" TextWrapping="Wrap">
            印刷ジョブは <Run FontWeight="Bold" Text="C:\VirtualPrintDemo"/>
            に1ページ毎の PNG として保存されます。
        </TextBlock>

        <TextBox x:Name="LogBox" Grid.Row="2" Margin="0,12,0,0"
                 IsReadOnly="True" VerticalScrollBarVisibility="Auto"
                 FontFamily="Consolas" FontSize="11"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="出力フォルダを開く" Padding="12,4"
                    Click="OpenOutputFolder_Click"/>
            <Button Content="閉じる" Padding="12,4" Margin="8,0,0,0"
                    Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>
